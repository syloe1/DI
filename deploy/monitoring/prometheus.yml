global:
  scrape_interval: 15s #抓取间隔
  evaluation_interval: 15s #规则计算间隔

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: go-admin
    static_configs:
      - targets:
          - host.docker.internal:6060
        labels:
          service: go-admin
